<%= form_with(model: banheiro) do |form| %>
  <% if banheiro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(banheiro.errors.count, "error") %> prohibited this banheiro from being saved:</h2>

      <ul>
        <% banheiro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :item, style: "display: block" %>
    <%= form.text_field :item %>
  </div>

  <div>
    <%= form.label :comprado, style: "display: block" %>
    <%= form.check_box :comprado %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
